# Optional explicit isolated PostgreSQL target.
# The integration runner otherwise derives <development_database>_test.
TEST_DATABASE_URL=postgresql://ai_exam:replace-with-local-password@127.0.0.1:5432/ai_exam_test

# PostgreSQL maintenance database used only to create and drop the isolated target.
POSTGRES_ADMIN_DATABASE=postgres
